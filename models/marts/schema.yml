version: 2

models:
  - name: customer_orders
    columns:
      - name: customer_id
        data_type: int
      - name: first_name
        data_type: string
      - name: last_name
        data_type: string
      - name: orders_count
        data_type: bigint
      - name: last_order_amount
        data_type: decimal(18,2)